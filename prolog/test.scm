(define (junk)
  (prove6 (link l g r e n c)
          (append3 (cdr a) (list (quote r!) l)  (cdr g))
          whole-db
          e*
          (+ 1 n)
          l
	  whole-db))
(define (clear_result) (set! result_ '()))
(newline)

